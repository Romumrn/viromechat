# Dependencies for the Streamlit client (app.py) only — used by docker/Dockerfile.app.
# For local, non-Docker dev running both processes on one host, use the
# combined requirements/all.txt instead.
-r base.txt
streamlit>=1.53
streamlit-authenticator>=0.4,<0.5
PyYAML>=6.0
# Imported directly by app.py (password verification) and db.py; also a
# transitive dependency of streamlit-authenticator, pinned here to be explicit.
bcrypt>=4.0
