# Secrets for Streamlit Community Cloud deployment ONLY.
# For LOCAL development, use .env.app instead (see .env.app.example) —
# this file is now gitignored, so fill in real values here only when
# deploying, never for local dev.

# ALBERT_API_KEY = "sk-..."
# PASSWORD_ENABLED = true
# ACCESS_CODE = "xxxxx"
