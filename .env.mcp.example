# Copy to .env.mcp and fill in — loaded by server_mcp.py (MCP server), for
# the S3-compatible bucket hosting the virus-host Parquet dataset.

ENDPOINT=
ACCESS_KEY=
SECRET_KEY=
BUCKET=
VIRAL_HOST_DATASET=

# Optional — defaults shown below are used if omitted:
# REGION=fr
# S3_URL_STYLE=path
